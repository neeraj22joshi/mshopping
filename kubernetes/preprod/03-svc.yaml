apiVersion: v1
kind: Service
metadata:
  name: preprod-mshopping-service
  namespace: preprod-mshopping
  labels:
    env: preprod-mshopping
    app: mshopping
    layer: frontend
spec:
  type: ClusterIP
  selector:
    app: mshopping
  ports:
    - name: http
      port: 8080
      targetPort: 8080