apiVersion: apps/v1
kind: Deployment
metadata:
  name: preprod-mshopping-deployment
  namespace: preprod-mshopping
  labels:
    app: mshopping
    env: preprod-mshopping
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 25%
      maxSurge: 100%
  selector:
    matchLabels:
      app: mshopping
      env: preprod-mshopping
  template:
    metadata:
      labels:
        app: mshopping
        env: preprod-mshopping
    spec:
      serviceAccountName: preprod-mshopping-service-sa
      containers:
        - name: mshopping
          image: 279078306727.dkr.ecr.ap-south-1.amazonaws.com/mshopping:<latest>
          resources:
            requests:
              memory: 256Mi
              cpu: 200m
            limits:
              memory: 512Mi
              cpu: 500m
          ports:
            - name: api
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /flights
              port: 8080
            initialDelaySeconds: 70
            periodSeconds: 10
            failureThreshold: 5

          livenessProbe:
            httpGet:
              path: /flights
              port: 8080
            initialDelaySeconds: 90
            periodSeconds: 20

          volumeMounts:
            - name: application-config
              mountPath: /app/config
      volumes:
        - name: application-config
          configMap:
            name: preprod-mshopping-deployment-application-config