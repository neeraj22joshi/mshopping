apiVersion: v1
kind: Service
metadata:
  name: prod-mshopping-service
  namespace: dev-mshopping
  labels:
    env: prod-mshopping
    app: mshopping
    layer: frontend
spec:
  type: ClusterIP
  selector:
    app: mshopping
  ports:
    - name: http
      port: 8080
      targetPort: 8080